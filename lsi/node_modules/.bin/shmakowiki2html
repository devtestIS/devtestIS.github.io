#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../shmakowiki/bin/shmakowiki2html" "$@"
  ret=$?
else 
  bash  "$basedir/../shmakowiki/bin/shmakowiki2html" "$@"
  ret=$?
fi
exit $ret
