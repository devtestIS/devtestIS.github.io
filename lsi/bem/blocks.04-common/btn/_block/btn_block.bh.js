module.exports = function (bh) {
    bh.match('btn', function (ctx, json) {
        // ctx.bem(false);
        // var oldCls = ctx.cls() || 'btn btn-default',
        //     val = 'btn-block';
        // ctx.cls(oldCls + ' ' + val, true)
    })
}
